/*
 if (draggedCount < 1 || targetCount < 1) {
    showAlert("You need at least 1 item from each stack!");
    return false;
}
if (freeSlots.length < 2) {
    showAlert("Not enough free slots for stack remains!");
    return false;
}
*/